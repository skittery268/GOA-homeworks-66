1) res.cookie გამოყენებით დააბრუნეთ ტოკენი გაუწერეთ ოთხი პარამეტრი, 
maxAge, secure, httpOnly და sameSite ახსენით კომენტარებტ რას აკეთღებს 
თითოეული

maxAge - cookies სექციაში შენახვის ვადა
secure - გადაეცემა ან false ან true, თუ false - ია მაშინ მოთხოვნა 
შეიძლება გამოიგზავნოს http - პროტოკოლით მაგრამ თუ true - მაშინ მარტო 
htpps პროტოკოლით.
httpOnly - თუ true ექნება გაწერილი მაშინ გამოყენებული იქნება ინფორმაციის 
გადაცემისთვის მარტო htpp.
sameSite - როგორი დომეინით შესაძლებელია მოთხოვნის გამოგზავნა.

2) დაინსტალირეთ cors და cookie-parser შემდეგ გამოიყენეთ cors და გადაეცით 
ობიექტი use მეთოდის დახმარებით, ობიექტში გაუწერეთ origin: * და 
credentials: true, შემდეგ use მეთოდის დახმარებიუთ გამოიყენეთ 
cookie-parser რომ გამოგზავნილი cookie req ობიექტში მარტივად ჩაჯდეს, 
საბოლოოდ შეცვალეთ protectფუნქცია და body ნაცვლად ტოკენი ამოიღეთ cookie 
სექციიდან

3) შექმენით client vite დახმარებით, შექმენიტ ავტორიზაციისა და რეგისტრაციის 
გვერდები თავისი კონტექსტით და ფუნქციებიეთ, მომხმარებლის წარმატებით 
ავტორიზაციის შემთხვევაში გადაიყვანეთ პროფილის გვერდზე.
4) შექმენით პოსტის ატვირთის ფუნქცია პირდაპირ პროფილიდან სადაც მომხმარებელს 
შეეძლება პოსტის დამატება/წაშლა/შეცვლა.

აუცილებელია თან გააგზავნოთ მომხმარებლის ტოკენი რომელიც ინახება cookie 
სექციაში 

req object add

credentials: include; 