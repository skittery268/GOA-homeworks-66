; 1) მოიძიეთ ინფორმაცია OSI მოდელზე და ნახეთ რა ფაზას გადის ჩვენი მოთხოვნები ერთი კომპიუტერიდან მეორე კომპიუტერთან მისასვლელად, კონკრეტულად განიხილეთ 7 ფენა იგივე Application

; OSI - Open Systems Interconnection Model არის მოდელი, რომელიც აღწერს კომპიუტერულ ქსელებში კომუნიკაციის პროცესს შვიდ ფენად დაყოფით. თითოეული ფენა პასუხისმგებელია კონკრეტულ ფუნქციებზე და უზრუნველყოფს მონაცემების გადაცემას ერთი კომპიუტერიდან მეორეზე.

; OSI იყოფა 7 ფენად:
; 1. ფიზიკური ფენა (Physical Layer);
; 2. მონაცემთა კავშირის ფენა (Data Link Layer);
; 3. ქსელის ფენა (Network Layer);
; 4. ტრანსპორტის ფენა (Transport Layer);
; 5. სესიის ფენა (Session Layer);
; 6. პრეზენტაციის ფენა (Presentation Layer);
; 7. აპლიკაციის ფენა (Application Layer).

; aplicaition ფენა არის ყველაზე მაღლა მდგომი ფენა OSI მოდელში და ის უზრუნველყოფს პირდაპირი კომუნიკაციის საშუალებას მომხმარებელთა პროგრამებსა და ქსელის სერვისებს შორის. როდესაც მომხმარებელი იწყებს მოთხოვნას, მაგალითად ვებ-ბრაუზერში, ეს მოთხოვნა იწყება აპლიკაციის ფენიდან.

